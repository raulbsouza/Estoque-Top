server:
  port: 8080
spring:
  application:
    name: books
  datasource:
    url: jdbc:postgresql://localhost:5432/estoque?serverTimeZone=ADT
    username: postgres
    password: 13951
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
spring-doc:
  api-docs:
    path: /api-docs
    pathsToMatch: /api/**
    swagger-ui:
      use-root-path: true